[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "xzpace"
version = "0.0.1"
description = "XZPACE Pricing Dashboard - Custom ERPNext App"
authors = [{name = "XZPACE", email = "info@xzpace.com"}]
requires-python = ">=3.8"
license = {text = "MIT"}
